# 🔄 VolunTech - Application Flow Diagrams

## Table of Contents
1. [User Authentication Flow](#user-authentication-flow)
2. [Event Lifecycle Flow](#event-lifecycle-flow)
3. [Rating & Review Flow](#rating--review-flow)
4. [Certificate Generation Flow](#certificate-generation-flow)
5. [Virtual Event Flow](#virtual-event-flow)
6. [Real-time Chat Flow](#real-time-chat-flow)
7. [Navigation Flow by Role](#navigation-flow-by-role)

---

## 1. User Authentication Flow

### Registration Flow
```
┌──────────────┐
│ Landing Page │
│  (index.tsx) │
└──────┬───────┘
       │
       ├─ Click "Sign Up"
       ▼
┌──────────────────────────┐
│  Signup Screen           │
│  - Name, Email, Password │
│  - Role Selection        │
└──────┬───────────────────┘
       │
       ├─ POST /api/auth/signup
       ▼
┌──────────────────────────────┐
│  Backend: Create User        │
│  - Hash password (bcrypt)    │
│  - hasCompletedOnboarding=F  │
│  - emailVerified=false       │
└──────┬───────────────────────┘
       │
       ├─ Send Verification Email
       ▼
┌──────────────────────────┐
│  Email Verification      │
│  - Enter 6-digit code    │
└──────┬───────────────────┘
       │
       ├─ POST /api/auth/verify-email
       ▼
┌──────────────────────────┐
│  Backend: Mark Verified  │
│  emailVerified = true    │
└──────┬───────────────────┘
       │
       ├─ Redirect to Login
       ▼
┌──────────────────────────┐
│  Login Screen            │
│  - Email & Password      │
└──────┬───────────────────┘
       │
       ├─ POST /api/auth/login
       ▼
┌───────────────────────────────┐
│  Backend: Verify Credentials  │
│  - bcrypt.compare()           │
│  - Generate JWT token         │
│  - payload: {sub:userId,...}  │
└──────┬────────────────────────┘
       │
       ├─ Return { token, user, needsOnboarding }
       ▼
┌────────────────────────────┐
│  AuthContext.login()       │
│  - Store token in AsyncSto │
│  - Set user state          │
└──────┬─────────────────────┘
       │
       ├─ Check needsOnboarding
       ▼
    ┌──┴──┐
    │ YES │
    └──┬──┘
       │
       ▼
┌──────────────────────┐
│ Interest Selection   │
│ (interest.tsx)       │
│ - Choose interests   │
└──────┬───────────────┘
       │
       ├─ PATCH /api/auth/onboarding
       ▼
    ┌──┴──┐
    │ NO  │
    └──┬──┘
       │
       ├─ Route by Role
       ▼
  ┌────┴────┬────────────┬────────┐
  │         │            │        │
  ▼         ▼            ▼        ▼
Volunteer Organization  Admin   Guest
Dashboard   Dashboard  Dashboard  View
```

### Login Flow (Simplified)
```
Login → Verify Credentials → Generate JWT → 
  Store Token → Check Onboarding → Route by Role
```

---

## 2. Event Lifecycle Flow

### Complete Event Journey

```
┌─────────────────────────────────────────────────────────────┐
│                     ORGANIZATION SIDE                        │
└─────────────────────────────────────────────────────────────┘

Step 1: CREATE EVENT
┌──────────────────────────┐
│  Organization Dashboard  │
│  "Create Event" button   │
└──────┬───────────────────┘
       │
       ├─ Fill Event Form
       │  - Title, Description
       │  - Date, Time, Location
       │  - Max Participants
       │  - Event Type, Difficulty
       ▼
┌──────────────────────────┐
│  POST /api/events        │
└──────┬───────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  Backend: Save Event          │
│  - status: "upcoming"         │
│  - currentParticipants: 0     │
│  - organizationId: user.id    │
└──────┬────────────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  Event appears in:            │
│  - Org event list             │
│  - Public event discovery     │
└───────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      VOLUNTEER SIDE                          │
└─────────────────────────────────────────────────────────────┘

Step 2: DISCOVER & JOIN
┌──────────────────────────┐
│  Volunteer Explore Page  │
│  - Browse events         │
│  - Filter by interest    │
└──────┬───────────────────┘
       │
       ├─ Click "Join Event"
       ▼
┌──────────────────────────────────┐
│  POST /api/events/:id/join       │
│  { userId }                      │
└──────┬───────────────────────────┘
       │
       ▼
┌───────────────────────────────────────┐
│  Backend: Create EventParticipant     │
│  - eventId, userId                    │
│  - status: "registered"               │
│  - attended: false                    │
└──────┬────────────────────────────────┘
       │
       ├─ Event.currentParticipants++
       ▼
┌───────────────────────────────┐
│  Event appears in:            │
│  - Volunteer "My Events"      │
│  - Personal calendar          │
└───────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   EVENT DAY (Organization)                   │
└─────────────────────────────────────────────────────────────┘

Step 3: MARK ATTENDANCE
┌──────────────────────────────────┐
│  Organization Event Details      │
│  - View registered volunteers    │
│  - "Mark Attendance" button      │
└──────┬───────────────────────────┘
       │
       ├─ Select volunteers present
       ▼
┌────────────────────────────────────────────┐
│  PATCH /api/events/:id/attendance/:userId  │
│  { status: "attended", checkInTime }       │
└──────┬─────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│  Backend: Update EventParticipant   │
│  - attended: true                   │
│  - checkInTime: timestamp           │
└──────┬──────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Volunteer now eligible to:          │
│  - Rate the event                    │
│  - Write a review                    │
│  - Receive certificate               │
└──────────────────────────────────────┘

Step 4: POST-EVENT ACTIVITIES
   ┌─────────────┴─────────────┐
   │                           │
   ▼                           ▼
[Volunteer]              [Organization]
Rate & Review            Award Certificates
   │                           │
   └──────────┬────────────────┘
              │
              ▼
       Event Complete!
```

---

## 3. Rating & Review Flow

```
┌──────────────────────────────────┐
│  Volunteer Activity History      │
│  - Shows attended events         │
│  - "Rate & Review" button        │
└──────┬───────────────────────────┘
       │
       ├─ Click "Rate & Review"
       ▼
┌─────────────────────────────────────┐
│  Check Attendance Status            │
│  GET /api/events/:id/my-review      │
└──────┬──────────────────────────────┘
       │
    ┌──┴───────────┐
    │ Not Attended │
    └──┬───────────┘
       │
       ▼
┌──────────────────────┐
│  Show Error:         │
│  "Must be marked     │
│   attended to rate"  │
└──────────────────────┘

    ┌─────────┐
    │Attended │
    └──┬──────┘
       │
       ▼
┌──────────────────────────────────┐
│  Open RateReviewModal            │
│  - Star rating (1-5)             │
│  - Review text (optional)        │
│  - Select badges (max 3)         │
└──────┬───────────────────────────┘
       │
       ├─ Validate Input
       │  ✓ Rating required
       │  ✓ Review 10-500 chars (optional)
       │  ✓ Max 3 badges
       ▼
┌─────────────────────────────────────┐
│  POST /api/events/:id/review        │
│  {                                  │
│    rating: 5,                       │
│    reviewText: "Great event!",      │
│    badges: ["excellence", "impact"] │
│  }                                  │
└──────┬──────────────────────────────┘
       │
       ▼
┌────────────────────────────────────────────┐
│  Backend: reviewController.js              │
│  1. Verify JWT token (req.user.id)         │
│  2. Check EventParticipant.attended=true   │
│  3. Create/Update EventReview document     │
│  4. Recalculate Event.ratings.average      │
│  5. Update Event.ratings.breakdown         │
│  6. Add badges to User.badges[]            │
└──────┬─────────────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Success Response                  │
│  - Review saved                    │
│  - Badges awarded                  │
│  - Organization can see review     │
└────────────────────────────────────┘

EDIT FLOW (Within 24 Hours)
┌────────────────────────────┐
│  Edit existing review      │
│  - Same validation         │
└──────┬─────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Check: createdAt vs now        │
│  If < 24 hours → Allow edit     │
│  Else → Show "Edit period over" │
└─────────────────────────────────┘
```

### Rating Calculation Algorithm
```javascript
// When a new review is submitted
1. Get all reviews for the event
2. Calculate average rating:
   average = sum(all ratings) / total reviews
3. Update breakdown:
   {
     5: count of 5-star reviews,
     4: count of 4-star reviews,
     3: count of 3-star reviews,
     2: count of 2-star reviews,
     1: count of 1-star reviews
   }
4. Update Event.ratings and Event.reviewCount
```

---

## 4. Certificate Generation Flow

```
┌─────────────────────────────────────┐
│  Organization Volunteer List        │
│  - View attended volunteers         │
│  - "Award Certificate" button       │
└──────┬──────────────────────────────┘
       │
       ├─ Select volunteers + event
       ▼
┌──────────────────────────────────────┐
│  POST /api/certificates/award        │
│  {                                   │
│    volunteerIds: [id1, id2],         │
│    eventId: "event-123",             │
│    certificateType: "participation", │
│    message: "Thank you!"             │
│  }                                   │
└──────┬───────────────────────────────┘
       │
       ▼
┌────────────────────────────────────────────┐
│  Backend: certificateController.js         │
│  For each volunteer:                       │
│    1. Generate unique certificate ID (UUID)│
│    2. Create certificate object:           │
│       {                                    │
│         id, eventId, volunteerId,          │
│         organizationId, message,           │
│         certificateType, style, color,     │
│         uniqueIdentifier: UUID,            │
│         awardedAt: Date.now()              │
│       }                                    │
│    3. Add to User.certificates[]           │
└──────┬─────────────────────────────────────┘
       │
       ▼
┌───────────────────────────────────┐
│  Certificate stored in MongoDB    │
│  Volunteer notified (optional)    │
└──────┬────────────────────────────┘
       │
       ▼

VOLUNTEER VIEW
┌───────────────────────────────────┐
│  Volunteer Profile                │
│  - "Certificates" section         │
│  - Shows all certificates         │
└──────┬────────────────────────────┘
       │
       ├─ Click "View/Download"
       ▼
┌────────────────────────────────────────┐
│  GET /api/certificates/generate/       │
│      :volunteerId/:certificateId       │
└──────┬─────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Backend: Generate Certificate Data     │
│  - Fetch certificate from User model    │
│  - Return certificate details           │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Frontend: CertificatePDFGenerator      │
│  - Render certificate design            │
│  - Generate PDF (react-native-view-shot)│
│  - Option to share/download             │
└─────────────────────────────────────────┘

VERIFICATION FLOW (Public)
┌───────────────────────────────┐
│  Anyone with certificate ID   │
└──────┬────────────────────────┘
       │
       ├─ GET /api/certificates/verify/:id
       ▼
┌────────────────────────────────────┐
│  Backend: Search all User models   │
│  for certificate with matching ID  │
└──────┬─────────────────────────────┘
       │
    ┌──┴───┐
    │Found │
    └──┬───┘
       │
       ▼
┌──────────────────────────────────┐
│  Return:                         │
│  - Volunteer name                │
│  - Event name                    │
│  - Organization name             │
│  - Date awarded                  │
│  - Status: "Valid"               │
└──────────────────────────────────┘

    ┌──────────┐
    │Not Found │
    └──┬───────┘
       │
       ▼
┌──────────────────────────────┐
│  Return:                     │
│  - Status: "Invalid"         │
│  - Message: "Not found"      │
└──────────────────────────────┘
```

---

## 5. Virtual Event Flow

```
┌────────────────────────────────────┐
│  Organization Virtual Hub          │
│  "Create Virtual Event" button     │
└──────┬─────────────────────────────┘
       │
       ├─ Fill Virtual Event Form
       │  - Title, Description
       │  - Date, Time, Duration
       │  - Platform (in-app/zoom/meet)
       │  - Google Meet Link (if external)
       ▼
┌──────────────────────────────────┐
│  POST /api/virtual/events        │
└──────┬───────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Backend: Create VirtualEvent      │
│  - status: "scheduled"             │
│  - currentParticipants: 0          │
│  - tasks: []                       │
└──────┬───────────────────────────┘
       │
       ▼
[Volunteers Join]
       │
       ├─ POST /api/virtual/events/:id/join
       ▼
┌────────────────────────────────────┐
│  Organization Creates Tasks        │
│  POST /api/virtual/events/:id/tasks│
│  {                                 │
│    title: "Design poster",         │
│    assignedTo: volunteerId,        │
│    priority: "high",               │
│    dueDate: "2024-12-01"           │
│  }                                 │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Task added to VirtualEvent.tasks[]│
│  Volunteer notified                │
└──────┬─────────────────────────────┘
       │
       ▼
[Volunteer Completes Task]
       │
       ├─ Upload files/links as output
       ▼
┌────────────────────────────────────────┐
│  POST /api/virtual/events/:id/tasks/   │
│       :taskId/outputs                  │
│  - Upload files (multer)               │
│  - Add links                           │
└──────┬─────────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Task.outputs[] updated            │
│  - fileName, fileUrl, uploadedAt   │
│  - uploadedBy: volunteerId         │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Organization Reviews Output       │
│  - Marks task as completed         │
│  PATCH /api/virtual/events/:id/    │
│        tasks/:taskId                │
│  { status: "completed" }           │
└────────────────────────────────────┘

EVENT START (Video Call)
┌────────────────────────────────────┐
│  POST /api/virtual/events/:id/start│
│  - status: "live"                  │
└──────┬─────────────────────────────┘
       │
    ┌──┴──────────────────┐
    │In-App Video Call    │
    └──┬──────────────────┘
       │
       ├─ Socket.IO: emit('video:join-room', eventId)
       ▼
┌─────────────────────────────────────────┐
│  Socket Handler (socketHandler.js)      │
│  - User joins video:${eventId} room     │
│  - WebRTC signaling for peer connections│
│  - emit('video:user-joined') to others  │
└─────────────────────────────────────────┘

    ┌──────────────────────┐
    │External (Google Meet)│
    └──┬───────────────────┘
       │
       ├─ Click "Join Google Meet"
       ▼
┌────────────────────────────────┐
│  Open googleMeetLink in browser│
└────────────────────────────────┘
```

---

## 6. Real-time Chat Flow

```
USER CONNECTS
┌──────────────────────────┐
│  User opens chat screen  │
└──────┬───────────────────┘
       │
       ├─ Get JWT token from AsyncStorage
       ▼
┌────────────────────────────────────┐
│  io.connect(API_BASE_URL, {        │
│    auth: { token }                 │
│  })                                │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  Backend: authenticateSocket       │
│  - Verify JWT token                │
│  - Extract userId from token       │
│  - socket.userId = userId          │
└──────┬─────────────────────────────┘
       │
       ▼
┌────────────────────────────────────┐
│  socket.join(`user:${userId}`)     │
│  - Personal room for notifications │
└────────────────────────────────────┘

JOIN CONVERSATION
┌────────────────────────────────────┐
│  Select conversation from list     │
└──────┬─────────────────────────────┘
       │
       ├─ emit('join:conversation', conversationId)
       ▼
┌─────────────────────────────────────┐
│  Backend: Verify user is participant│
│  - Check Conversation.participants  │
└──────┬──────────────────────────────┘
       │
    ┌──┴────┐
    │Valid  │
    └──┬────┘
       │
       ▼
┌────────────────────────────────────────┐
│  socket.join(`conversation:${id}`)     │
│  emit('joined:conversation', { id })   │
└────────────────────────────────────────┘

SEND MESSAGE
┌────────────────────────────────────┐
│  User types message and hits send  │
└──────┬─────────────────────────────┘
       │
       ├─ emit('message:send', {
       │    conversationId,
       │    content,
       │    type: 'text'
       │  })
       ▼
┌─────────────────────────────────────┐
│  Backend: Create Message document   │
│  - id: UUID                         │
│  - conversationId                   │
│  - senderId, senderName, senderAvat │
│  - content, timestamp               │
└──────┬──────────────────────────────┘
       │
       ├─ Save to MongoDB
       ▼
┌─────────────────────────────────────────┐
│  Update Conversation.lastMessage        │
│  - lastMessageContent                   │
│  - lastMessageTime                      │
└──────┬──────────────────────────────────┘
       │
       ├─ Broadcast to conversation room
       ▼
┌──────────────────────────────────────────┐
│  io.to(`conversation:${id}`)             │
│    .emit('message:new', message)         │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  All connected users in conversation     │
│  receive message instantly               │
└──────────────────────────────────────────┘

       │
       ├─ Notify offline participants
       ▼
┌──────────────────────────────────────────┐
│  io.to(`user:${otherUserId}`)            │
│    .emit('notification:message', {       │
│      conversationId, message, unreadCount│
│    })                                    │
└──────────────────────────────────────────┘

TYPING INDICATOR
┌────────────────────────────────────┐
│  User starts typing                │
└──────┬─────────────────────────────┘
       │
       ├─ emit('typing:start', conversationId)
       ▼
┌─────────────────────────────────────────┐
│  socket.to(`conversation:${id}`)        │
│    .emit('typing:user', {               │
│      userId, userName, isTyping: true   │
│    })                                   │
└─────────────────────────────────────────┘

       │ (After 3 seconds of no typing)
       ├─ emit('typing:stop', conversationId)
       ▼
┌─────────────────────────────────────────┐
│  socket.to(`conversation:${id}`)        │
│    .emit('typing:user', {               │
│      userId, userName, isTyping: false  │
│    })                                   │
└─────────────────────────────────────────┘

READ RECEIPT
┌────────────────────────────────────┐
│  User views message                │
└──────┬─────────────────────────────┘
       │
       ├─ emit('message:read', { messageId, conversationId })
       ▼
┌─────────────────────────────────────┐
│  Backend: Update Message.readBy[]   │
│  - Add userId to readBy array       │
└──────┬──────────────────────────────┘
       │
       ├─ Notify sender
       ▼
┌─────────────────────────────────────────┐
│  io.to(`user:${senderId}`)              │
│    .emit('message:read', {              │
│      messageId, conversationId, userId  │
│    })                                   │
└─────────────────────────────────────────┘
```

---

## 7. Navigation Flow by Role

### Volunteer Navigation
```
Login as Volunteer
       │
       ▼
/volunteer (entry screen)
       │
       ▼
(volunteerTabs)/_layout.tsx (Bottom Tabs)
       │
       ├─────────────────┬──────────────┬─────────────┬──────────────┬──────────────┐
       │                 │              │             │              │              │
       ▼                 ▼              ▼             ▼              ▼              ▼
    home.tsx        explore.tsx    calendar.tsx  emergency.tsx  crowdfunding.tsx virtualhub.tsx
       │                 │              │             │              │              │
       │                 │              │             │              │              │
  ┌────┴────┐      ┌────┴────┐    ┌───┴────┐   ┌────┴────┐    ┌───┴────┐    ┌───┴────┐
  │ Event   │      │ Browse  │    │ My     │   │ Report  │    │ Browse │    │ Online │
  │ Feed    │      │ Events  │    │ Events │   │ Emergency│   │ Causes │    │ Events │
  │         │      │ Join    │    │ View   │   │ Alerts   │   │ Donate │    │ Tasks  │
  │ Quick   │      │ Filter  │    │ Details│   │         │    │        │    │        │
  │ Actions │      │         │    │        │   │         │    │        │    │        │
  └─────────┘      └─────────┘    └────────┘   └─────────┘    └────────┘    └────────┘

From any tab → Click Profile Icon
       │
       ▼
/myprofile.tsx
       │
       ├─ View Profile
       ├─ Edit Profile (EditProfileModal)
       ├─ Activity History (EnhancedActivityHistory)
       ├─ Badges (EnhancedBadgesSection)
       ├─ Certificates (EnhancedCertificatesSection)
       ├─ Rate Events (RateReviewModal)
       └─ Logout
```

### Organization Navigation
```
Login as Organization
       │
       ▼
/organization (entry screen)
       │
       ▼
(organizationTabs)/_layout.tsx (Bottom Tabs)
       │
       ├──────────┬──────────┬──────────┬──────────┬──────────┐
       │          │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼          ▼
    home.tsx  volunteers calendar certificates impacttracker reports
       │          │          │          │          │          │
  ┌────┴────┐ ┌──┴────┐ ┌───┴────┐ ┌──┴────┐ ┌───┴────┐ ┌───┴────┐
  │ Events  │ │ Vol.  │ │ Event  │ │ Award │ │ Stats  │ │ Export │
  │ Create  │ │ List  │ │ Schedu │ │ Certif │ │ Volunt │ │ Data  │
  │ Manage  │ │ Status│ │ le     │ │ icates│ │ eers   │ │       │
  │ Edit    │ │ Check │ │        │ │       │ │ Impact │ │       │
  │ Delete  │ │ In/Out│ │        │ │       │ │ Metrics│ │       │
  └─────────┘ └───────┘ └────────┘ └───────┘ └────────┘ └────────┘
       │          │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼          ▼
   resources crowdfundingorg virtualhub
       │          │          │
  ┌────┴────┐ ┌──┴────┐ ┌───┴────┐
  │ Upload │ │ Create │ │ Virtual│
  │ Docs   │ │ Campgn │ │ Events │
  │ Guides │ │        │ │ Tasks  │
  └────────┘ └───────┘ └────────┘

From any tab → Profile
       │
       ├─ View Reviews (ReviewsList)
       ├─ View Badges Earned (OrganizationBadgesSection)
       └─ Edit Profile
```

### Admin Navigation
```
Login as Admin
       │
       ▼
/admin (entry screen)
       │
       ▼
(adminTabs)/_layout.tsx (Bottom Tabs)
       │
       ├──────────┬──────────┬──────────┬──────────┬──────────┐
       │          │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼          ▼
    home.tsx   users.tsx analytics categories emergency revenue
       │          │          │          │          │          │
  ┌────┴────┐ ┌──┴────┐ ┌───┴────┐ ┌──┴────┐ ┌───┴────┐ ┌───┴────┐
  │ Platfrm │ │ User  │ │ Charts │ │ Event │ │ Alerts │ │ Income │
  │ Overvw │ │ Mgmt  │ │ Metrics│ │ Types │ │ Manage │ │ Expense│
  │ Alerts │ │ Ban   │ │ Growth │ │ Add/Ed│ │        │ │        │
  └─────────┘ └───────┘ └────────┘ └───────┘ └────────┘ └────────┘
       │          │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼          ▼
subscriptions fees.tsx  ads.tsx  technical virtual
       │          │          │          │          │
  ┌────┴────┐ ┌──┴────┐ ┌───┴────┐ ┌──┴────┐ ┌───┴────┐
  │ Plans  │ │ Set   │ │ Ad     │ │ Server │ │ Virtual│
  │ Pricing│ │ Fees  │ │ Mgmt   │ │ Health │ │ Events │
  │        │ │ Commis│ │        │ │ Logs   │ │ Monitor│
  └────────┘ └───────┘ └────────┘ └────────┘ └────────┘
```

---

## 🔄 State Management Flow

### AuthContext Flow
```
App Start
   │
   ├─ AuthProvider initialized
   │
   ├─ checkStoredAuth()
   │    └─ AsyncStorage.getItem('auth_token')
   │    └─ AsyncStorage.getItem('auth_user')
   │
   ├─ If token exists:
   │    ├─ setUser(stored user)
   │    ├─ setToken(stored token)
   │    ├─ setIsAuthenticated(true)
   │    └─ Navigate to role dashboard
   │
   └─ If no token:
        ├─ setIsAuthenticated(false)
        └─ Navigate to login

Login Action
   │
   ├─ AuthContext.login(userData, rememberMe)
   │
   ├─ setUser(userData.user)
   ├─ setToken(userData.token)
   ├─ setIsAuthenticated(true)
   │
   ├─ If rememberMe:
   │    ├─ AsyncStorage.setItem('auth_token', token)
   │    ├─ AsyncStorage.setItem('auth_user', JSON.stringify(user))
   │    └─ AsyncStorage.setItem('remember_me', 'true')
   │
   └─ Navigate based on needsOnboarding & role

Logout Action
   │
   ├─ AuthContext.logout()
   │
   ├─ setUser(null)
   ├─ setToken(null)
   ├─ setIsAuthenticated(false)
   │
   ├─ AsyncStorage.removeItem('auth_token')
   ├─ AsyncStorage.removeItem('auth_user')
   ├─ AsyncStorage.removeItem('remember_me')
   │
   └─ Navigate to /login
```

---

## 📊 Data Sync Flow

### Event Data Sync
```
Volunteer Joins Event
   │
   ├─ Frontend: POST /api/events/:id/join
   │
   ├─ Backend: Update MongoDB
   │    ├─ Create EventParticipant
   │    └─ Event.currentParticipants++
   │
   ├─ Response: { success, participant }
   │
   ├─ Frontend: Update local state
   │    ├─ Add event to "My Events"
   │    └─ Update UI
   │
   └─ Other users see updated participant count
```

### Real-time Updates (Socket.IO)
```
User Action (e.g., Send Message)
   │
   ├─ emit('message:send', data) → Server
   │
   ├─ Server validates & saves to DB
   │
   ├─ Server broadcasts:
   │    io.to(`conversation:${id}`).emit('message:new', message)
   │
   ├─ All connected clients in room receive event
   │
   └─ Clients update UI instantly
```

---

## 🎯 Error Handling Flow

```
API Request
   │
   ├─ try {
   │    fetch(API_URL, options)
   │  }
   │
   ├─ Success (200-299)
   │    └─ Return data
   │
   ├─ Client Error (400-499)
   │    ├─ 401: Token expired → Logout → Redirect to login
   │    ├─ 403: Forbidden → Show "Access denied"
   │    ├─ 404: Not found → Show "Resource not found"
   │    └─ Other: Show error message
   │
   ├─ Server Error (500-599)
   │    └─ Show "Server error, try again"
   │
   └─ Network Error (no response)
        └─ Show "Check your connection"

Backend Error Handling
   │
   ├─ Try-Catch in controllers
   │
   ├─ Custom errors throw to global handler
   │
   ├─ middleware/errorHandler.js
   │    ├─ Log error (Winston)
   │    ├─ Determine status code
   │    └─ Send JSON response
   │
   └─ Client receives error
```

---

## 🔐 Security Flow

### JWT Token Lifecycle
```
1. User Login
   ├─ Backend generates JWT
   │    payload: { sub: userId, name, role, iat, exp }
   │    signed with JWT_SECRET
   │    expires in 7 days
   │
   └─ Token sent to client

2. Client Stores Token
   └─ AsyncStorage.setItem('auth_token', token)

3. Authenticated Requests
   ├─ Client includes token in header:
   │    Authorization: Bearer <token>
   │
   ├─ Backend middleware: authenticateToken()
   │    ├─ Extract token from header
   │    ├─ jwt.verify(token, JWT_SECRET)
   │    ├─ If valid: req.user = { id: payload.sub, ...payload }
   │    └─ If invalid: 403 Forbidden
   │
   └─ Controller accesses req.user.id

4. Token Expiry
   ├─ Token expires after 7 days
   ├─ Next request fails with 403
   └─ Client detects 403 → logout → redirect to login

5. Logout
   ├─ Client removes token from AsyncStorage
   └─ (Server is stateless, no session to destroy)
```

---

## 📄 Summary

This document visualizes all major flows in the VolunTech platform:
- ✅ User authentication and authorization
- ✅ Event creation, joining, and attendance
- ✅ Rating and review system
- ✅ Certificate generation and verification
- ✅ Virtual events and task management
- ✅ Real-time chat and WebRTC video
- ✅ Role-based navigation
- ✅ State management and data sync
- ✅ Error handling
- ✅ Security and JWT lifecycle

All flows are production-ready and follow industry best practices! 🚀

---

**Generated**: October 19, 2025
**Version**: 1.1.0

